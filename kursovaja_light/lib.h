#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

double min(vector<double> &);

double max(vector<double> &);

int last_positiv(vector<double> &);

double sum(vector<double> &);

double sum_until(vector<double> &, int);

void move(vector<double> &, int , int );

void compr(vector<double> &, double , double );
