#include <iostream>
#include <cmath>
#include <vector>
#include <cstdio>
#include <typeinfo>
#include <cstring>
#include <cctype>

using namespace std;

double maximum(vector<double> &);
int next_positive(vector<double> &, int);
double sum(vector<double> &, int, int);
double compare_and_print(vector<double> &);
void bring_zero_to_end(vector<double> &, int);
void print_sort_massive(vector<double> &);
